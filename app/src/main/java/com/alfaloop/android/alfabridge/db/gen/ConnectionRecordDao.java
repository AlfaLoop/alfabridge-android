package com.alfaloop.android.alfabridge.db.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.alfaloop.android.alfabridge.db.model.ConnectionRecord;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONNECTION_RECORD".
*/
public class ConnectionRecordDao extends AbstractDao<ConnectionRecord, Long> {

    public static final String TABLENAME = "CONNECTION_RECORD";

    /**
     * Properties of entity ConnectionRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property MacAddr = new Property(1, String.class, "macAddr", false, "MAC_ADDR");
        public final static Property UpdateDate = new Property(2, java.util.Date.class, "updateDate", false, "UPDATE_DATE");
    }


    public ConnectionRecordDao(DaoConfig config) {
        super(config);
    }
    
    public ConnectionRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CONNECTION_RECORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"MAC_ADDR\" TEXT NOT NULL UNIQUE ," + // 1: macAddr
                "\"UPDATE_DATE\" INTEGER NOT NULL );"); // 2: updateDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CONNECTION_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ConnectionRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMacAddr());
        stmt.bindLong(3, entity.getUpdateDate().getTime());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ConnectionRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getMacAddr());
        stmt.bindLong(3, entity.getUpdateDate().getTime());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public ConnectionRecord readEntity(Cursor cursor, int offset) {
        ConnectionRecord entity = new ConnectionRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // macAddr
            new java.util.Date(cursor.getLong(offset + 2)) // updateDate
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ConnectionRecord entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setMacAddr(cursor.getString(offset + 1));
        entity.setUpdateDate(new java.util.Date(cursor.getLong(offset + 2)));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ConnectionRecord entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(ConnectionRecord entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ConnectionRecord entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
